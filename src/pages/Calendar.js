import { Container, Typography } from '@mui/material';

function Calendar() {
  return (
    <Container>
      <Typography variant="h4" sx={{ mt: 4 }}>Calendar</Typography>
    </Container>
  );
}

export default Calendar;
